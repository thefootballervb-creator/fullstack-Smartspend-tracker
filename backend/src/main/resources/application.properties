server.port=9090

spring.main.allow-circular-references=true

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/expenses_tracker
spring.datasource.username=root
spring.datasource.password=123456789
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

app.jwtSecret=ZXhwZW5zZVRyYWNrZXJNeVdhbGxldFNlY3JldEtleUZvckpXVFRva2Vuc0VuY3J5cHRpb24xMjM0NTY3ODk=
app.jwtExpirationMs=86400000

# Email Provider Configuration
# Options: "smtp" (default) or "sendgrid"
# Set to "sendgrid" to use SendGrid API, "smtp" to use SMTP (Mailtrap, Gmail, etc.)
email.provider=smtp

# SMTP Configuration (used when email.provider=smtp)
# Mailgun SMTP Settings
# Get your SMTP credentials from: https://app.mailgun.com/
# 1. Go to Domain settings > SMTP credentials
# 2. Create SMTP credentials if you haven't already
# 3. Copy the username and password below
spring.mail.host=smtp.mailgun.org
spring.mail.port=587
spring.mail.username=YOUR_MAILGUN_SMTP_USERNAME
spring.mail.password=YOUR_MAILGUN_SMTP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Gmail SMTP Settings (commented out - use for production if needed)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=465
# spring.mail.username=balavvenkat930@gmail.com
# spring.mail.password=ncsgrikmjyutmxql
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.ssl.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=*
# spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
# spring.mail.properties.mail.smtp.starttls.enable=false
# spring.mail.properties.mail.smtp.starttls.required=false

# Mailtrap SMTP Settings (for testing - uncomment and use Mailtrap credentials)
# Get your credentials from: https://mailtrap.io/
# spring.mail.host=smtp.mailtrap.io
# spring.mail.port=2525
# spring.mail.username=your_mailtrap_username
# spring.mail.password=your_mailtrap_password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=false
# Enable SMTP debugging (set to false in production)
# Temporarily set to true to diagnose email sending issues
spring.mail.properties.mail.debug=true

# Sender Email Configuration
# For Mailgun: Use your verified sender email (from Mailgun dashboard)
# For sandbox: You can only send to authorized recipients
spring.mail.from.email=postmaster@sandboxf62c33d9063d405a8c44.mailgun.org
spring.mail.from.name=Expense Tracker

# SendGrid Configuration (used when email.provider=sendgrid)
# Get your API key from https://app.sendgrid.com/settings/api_keys
sendgrid.api.key=YOUR_SENDGRID_API_KEY_HERE
# The verified sender email address in your SendGrid account
sendgrid.from.email=noreply@yourdomain.com
# Optional: Custom sender name (defaults to "Company")
sendgrid.from.name=Company

app.verificationCodeExpirationMs=900000

app.user.profile.upload.dir=./uploads/user/profile
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration - Write SMTP debug logs to file
logging.file.name=backend.log
logging.level.com.sun.mail=DEBUG
logging.level.jakarta.mail=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.com.fullStack.expenseTracker.services.impls.EmailNotificationService=DEBUG
logging.level.com.fullStack.expenseTracker.services.impls.AuthServiceImpl=DEBUG
